<html>
    <head>
        <title>Similar Author Identification</title>
        <link rel="stylesheet" href="http://netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css" />
        <link rel="stylesheet" href="http://netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap-theme.min.css" />
        <link rel="stylesheet" href="http://getbootstrap.com/examples/starter-template/starter-template.css" />

        <style type="text/css">
            body {
                text-align: center;
                overflow: hidden;
            }
            .blog-post-content {
                margin: 80;
            }
            #run-button {
                float: right;
                width: 100;
            }
            .result-content {
                font-size: 1.5em;
                text-align: center;
                background-color: rgb(12, 144, 136);
                margin-left: 10%;
                margin-right: 10%;
                padding: 20px;
                color: white;
                border: 2px solid;
                border-radius: 25px;
                clear: left;
                position: relative;
            }
            .cluster-label-div {
                clear: both;
                text-align: left;
                margin-left: 20%;
            }
            #cluster_view {
                margin-right: 80;
                float: right;
            }
            .method {
                text-align: left;
            }

        </style>

    </head>

    <body>

    <a href="/clusters" id="cluster_view">View all clusters</a>

    <h1>Similar Author Identification</h1>

    <div class="blog-post-content">

        <form class="form-horizontal" role="form" id='blog-post-form' method='post' action='/'>

            <div class="form-group">
                <h4 class="control-label" style="float: left">Blog Post (in German)</h4>
                <br/><br/>
                <div>
                    <textarea class="form-control" cols="20" rows="10" id="blog-post" name='blog-post' placeholder="Enter a blog post..." required="required">{{blog-post}}</textarea>
                </div>
            </div>

            <br/>

            <div class="method">
                Cluster determination method: <br/>
                <input type="radio" name="method" value="euclidean" {{#euclidean}} checked="true" {{/euclidean}} > euclidean distance</input> <br/>
                <input type="radio" name="method" value="k-nearest" {{#k-nearest}} checked="true" {{/k-nearest}} > k-nearest neighbor</input> <br/>
                <input type="radio" name="method" value="svm" {{#svm}} checked="true" {{/svm}} > support vector machine</input>
            </div>

            <br/>

            <input type='submit' value='Run' class="btn btn-primary" id="run-button" form='blog-post-form' />
        </form>
    </div>

    {{#result}}
        <hr/>

        <div class="result-content">
            <h2>Result</h2>
            <br/>
            The blog post belongs to cluster # {{cluster-number}}.
            <br/><br/>
            <div class="cluster-label-div">
                <span style="text-decoration: underline">The cluster</span>
                <ul>
                {{#cluster-labels}}
                    <li>{{.}}
                {{/cluster-labels}}
                </ul>
            </div>
        </div>
    {{/result}}

    </body>
</html>
